import React from 'react';

const App: React.FC = () => {
    return (
        <div>
            <h1>Hello, World!</h1>
        </div>
    );
};

export default App; // Ensure this is a default export